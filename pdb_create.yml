
- name: 'PDB Create'
  hosts: '{{ hostgroup }}'
  gather_facts: 'no'
  
  tasks:
    
    - include_vars:
        file: './vars/{{ item }}'
      loop: '{{ vars_list }}'
    
    - name: 'Create PDB'
      include_role:
        name: 'pdb'
